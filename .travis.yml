language: erlang
branches:
  only:
    - master
notifications:
  email: arcusfelis@gmail.com
  irc: 
install: rebar3 compile
script: rebar3 ct --config=azdht_test.cfg

matrix:
  include:
    - otp_release: 22.0
    - otp_release: 21.3
